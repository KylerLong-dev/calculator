const firstNumber = "";
const secondNumber = "";
const operator = "";

const toAdd = function (numOne, numTwo) {
    return numOne + numTwo; 
}

const toSubtract = function (numOne, numTwo) {
    return numOne - numTwo
}

const toMultiply = function (numOne, numTwo) {
    return numOne * numTwo;
}

const toDivide = function (numOne, numTwo) {
    return numOne / numTwo; 
}

const operate = function () {
    
}